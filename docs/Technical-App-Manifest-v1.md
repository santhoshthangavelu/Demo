# Technical Application Manifest — v1
> **Template:** `Technical-App-Manifest-v1` | **Schema parsed from:** Confluence → "Templates" space
> **Auto-generated by:** Repo Intelligence Agent | **Source branch:** `swaglabs-clone`

---

## Section 1 — Executive Summary

| Placeholder | Resolved Value |
|---|---|
| `{App_Name}` | **Swag Labs (SauceDemo)** |
| `{Service_Owner}` | Sauce Labs QA & Dev Enablement Group |
| `{Business_Impact}` | **Critical** — Global Demonstration Platform |
| `{Description}` | High-fidelity React SPA mirroring the SauceDemo user flow and route map for QA, demo, and technical manifest extraction use cases. |

---

## Section 2 — System Architecture & Tech Stack

| Placeholder | Resolved Value |
|---|---|
| `{Language_Runtime}` | JavaScript / Node 24 |
| `{Frameworks}` | React 17, React Router DOM v5, Vite 5 |
| `{Primary_Database}` | None — all data is mocked locally (`src/data/products.js`) |
| `{Cloud_Provider}` | AWS (S3 static hosting) |
| `{Infrastructure}` | Multi-stage Docker build → Nginx Alpine; GitHub Actions CI/CD; AWS S3 static delivery |
| `{Tech_Stack}` | React 17 · Node 24 · Vite · Backtrace SDK · Nginx · Docker |

### Dependency Matrix

| Package | Type | Version |
|---|---|---|
| `react` | Runtime | ^17.0.2 |
| `react-dom` | Runtime | ^17.0.2 |
| `react-router-dom` | Runtime | ^5.3.4 |
| `@backtrace-labs/react` | Runtime / Observability | ^1.7.0 |
| `vite` | Build | ^5.4.2 |
| `@vitejs/plugin-react` | Build | ^4.3.1 |
| `vitest` | Dev / Test | ^2.1.1 |
| `cypress` | Dev / E2E | ^13.13.2 |
| `snyk` | Dev / Security | ^1.1298.0 |
| `@testing-library/react` | Dev / Test | ^16.0.1 |
| `@testing-library/jest-dom` | Dev / Test | ^6.6.3 |
| `@testing-library/user-event` | Dev / Test | ^14.5.2 |

---

## Section 3 — Integration & Dependencies

### Upstream Dependencies
| Placeholder | Resolved Value |
|---|---|
| `{Upstream}` | SauceDemo experience (source-of-truth for UX and route design) |

### Downstream Dependencies
| Placeholder | Resolved Value |
|---|---|
| `{Downstream}` | AWS S3 static hosting; containerised Nginx server (port 80) |

### External APIs
| Placeholder | Resolved Value |
|---|---|
| `{External_APIs}` | Backtrace error-reporting API (token: `VITE_BACKTRACE_TOKEN` env var) |

### Entry Points (`{Entry_Points}`)

| Route | Component | Notes |
|---|---|---|
| `/` | `Login` | Default entry — credential validation |
| `/index.html` | `Login` | Alias for root |
| `/inventory.html` | `Inventory` | Product grid with sort; requires auth |
| `/cart.html` | `Cart` | Shopping cart view |
| `/checkout-step-one.html` | `CheckoutStepOne` | Checkout — contact info |
| `/checkout-step-two.html` | `CheckoutStepTwo` | Checkout — order review |
| `/checkout-complete.html` | `CheckoutComplete` | Order confirmation |
| `*` | `Redirect → /` | Catch-all fallback |

---

## Section 4 — Technical Configuration

| Placeholder | Resolved Value |
|---|---|
| `{Main_Branch}` | `swaglabs-clone` |
| `{Build_Tool}` | Vite (`npm run build` → `dist/`) |
| `{Critical_Env_Variables}` | `VITE_BACKTRACE_TOKEN`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION`, `AWS_S3_BUCKET` |
| `{Deployment_Pipeline}` | GitHub Actions (`deploy.yml`) — push to `swaglabs-clone` or `workflow_dispatch` |

### CI/CD Pipeline Steps
```
1. actions/checkout@v4
2. actions/setup-node@v4  →  Node 24, npm cache
3. npm ci                 →  Install dependencies
4. npm run test.coverage  →  Run Vitest coverage
5. npm run build          →  Vite production bundle → dist/
6. tar -czf *.tar.gz dist →  Package artifact
7. aws-actions/configure-aws-credentials@v4
8. aws s3 sync dist s3://$AWS_S3_BUCKET --delete
```

### Dev Server
- Port: `5173` (Vite local) | `80` (Nginx container)
- E2E base URL: `http://localhost:5173`

---

## Section 5 — Quality & Compliance

| Placeholder | Resolved Value |
|---|---|
| `{Test_Frameworks}` | **Unit:** Vitest 2 + Testing Library (jsdom) · **E2E:** Cypress 13 |
| `{Code_Coverage_Goal}` | Coverage reporters: `text` + `html` (via `npm run test.coverage`) |
| `{Security_Scanning}` | **Snyk** `^1.1298.0` — dependency vulnerability scanning |
| `{Observation_Logging}` | **Backtrace React SDK** `^1.7.0` — crash reporting & error tracking |

### Test Inventory

| Test File | Framework | Coverage |
|---|---|---|
| `src/components/Login.test.jsx` | Vitest + Testing Library | Locked-out user error path |
| `cypress/e2e/smoke.cy.js` | Cypress | Login page load & brand render |

### Vitest Configuration
```js
environment: 'jsdom' | globals: true | coverage.reporter: ['text', 'html']
```

---

## Section 6 — Documentation & Resources

| Placeholder | Resolved Value |
|---|---|
| `{GitHub_Repository}` | `santhoshthangavelu/Demo` (branch: `swaglabs-clone`) |
| `{API_Docs}` | N/A — No external REST/GraphQL API surface; all data is mocked |
| `{JIRA_Board}` | Not configured in repository metadata |
| `{On_Call_Rotation}` | Not configured in repository metadata |

---

## Section 7 — Deployment Status

| Placeholder | Resolved Value |
|---|---|
| `{Current_Version}` | `1.0.0` (from `package.json`) |
| `{Last_Updated}` | Auto-populated by CI pipeline on each push to `swaglabs-clone` |

---

## Schema Placeholder Map (Master Reference)

> Full inventory of all `{placeholder}` tokens parsed from the `Technical-App-Manifest-v1` template and their resolved status.

| Placeholder Token | Section | Status | Resolved Value |
|---|---|---|---|
| `{App_Name}` | 1 | ✅ Resolved | Swag Labs (SauceDemo) |
| `{Service_Owner}` | 1 | ✅ Resolved | Sauce Labs QA & Dev Enablement Group |
| `{Business_Impact}` | 1 | ✅ Resolved | Critical |
| `{Description}` | 1 | ✅ Resolved | React SPA / SauceDemo mirror |
| `{Language_Runtime}` | 2 | ✅ Resolved | JavaScript / Node 24 |
| `{Frameworks}` | 2 | ✅ Resolved | React 17, React Router DOM v5, Vite 5 |
| `{Primary_Database}` | 2 | ✅ Resolved | None (mocked data) |
| `{Cloud_Provider}` | 2 | ✅ Resolved | AWS |
| `{Infrastructure}` | 2 | ✅ Resolved | Docker + Nginx + GitHub Actions + S3 |
| `{Tech_Stack}` | 2 | ✅ Resolved | React · Node · Vite · Backtrace · Nginx |
| `{Upstream}` | 3 | ✅ Resolved | SauceDemo (UX/route source of truth) |
| `{Downstream}` | 3 | ✅ Resolved | AWS S3 / Nginx container |
| `{External_APIs}` | 3 | ✅ Resolved | Backtrace error-reporting API |
| `{Entry_Points}` | 3 | ✅ Resolved | 7 routes mapped (see table above) |
| `{Main_Branch}` | 4 | ✅ Resolved | swaglabs-clone |
| `{Build_Tool}` | 4 | ✅ Resolved | Vite |
| `{Critical_Env_Variables}` | 4 | ✅ Resolved | 5 secrets (see table above) |
| `{Deployment_Pipeline}` | 4 | ✅ Resolved | GitHub Actions deploy.yml |
| `{Test_Frameworks}` | 5 | ✅ Resolved | Vitest + Cypress |
| `{Code_Coverage_Goal}` | 5 | ✅ Resolved | text + HTML reporters |
| `{Security_Scanning}` | 5 | ✅ Resolved | Snyk |
| `{Observation_Logging}` | 5 | ✅ Resolved | Backtrace React SDK |
| `{GitHub_Repository}` | 6 | ✅ Resolved | santhoshthangavelu/Demo |
| `{API_Docs}` | 6 | ⚠️ N/A | No REST/GraphQL surface |
| `{JIRA_Board}` | 6 | ❌ Not configured | — |
| `{On_Call_Rotation}` | 6 | ❌ Not configured | — |
| `{Current_Version}` | 7 | ✅ Resolved | 1.0.0 |
| `{Last_Updated}` | 7 | ✅ CI-managed | On push to swaglabs-clone |
